# 模型分组功能说明

## 功能概述

模型分组（Model Groups）功能允许将多个模型按优先级顺序组合成一个统一的对外模型名，实现智能降级和故障转移。

## 核心特性

### 1. 分组配置
- **对外模型名**：客户端使用的统一模型名（如 `gpt-5-unified`）
- **分组名称**：便于管理的分组标识（如 `高性能GPT5组`）
- **成员模型**：按优先级排序的模型列表，支持拖拽排序
- **启用/禁用**：可随时切换分组状态

### 2. 路由策略（按需求实现）
- **按模型名选择**：成员是模型名（如 `gpt-5.6-sol`），非端点 ID
- **严格顺序降级**：按成员顺序依次尝试（1→2→3→4）
- **智能健康检测**：每个模型内部保留原有的健康层、优先级、粘连、冷却机制
- **完全兼容**：不影响现有的单模型路由和别名功能

### 3. 工作流程

```
客户端请求: model="gpt-5-unified"
  ↓
检测到分组: group-gpt-5
  ↓
成员模型: [gpt-5.6-sol, gpt-5.6-terra, gpt-5.5, gpt-5.4]
  ↓
1. 尝试 gpt-5.6-sol（所有该模型的端点）
   - 健康端点优先
   - 优先级排序
   - 粘连路由
   - 失败 → 下一个成员
  ↓
2. 尝试 gpt-5.6-terra
   ...
  ↓
直到成功或全部失败
```

## 使用示例

### 示例 1：高性能 GPT 组
```json
{
  "name": "高性能GPT5组",
  "public_model": "gpt-5-unified",
  "members": [
    "gpt-5.6-sol",     // 最优先，多个不同中转站的此模型都会尝试
    "gpt-5.6-terra",   // 次优先
    "gpt-5.5",         // 降级选项1
    "gpt-5.4"          // 降级选项2
  ]
}
```

客户端调用：
```bash
curl http://localhost:5100/v1/chat/completions \
  -H "Content-Type: application/json" \
  -d '{
    "model": "gpt-5-unified",
    "messages": [{"role": "user", "content": "Hello"}]
  }'
```

### 示例 2：Claude 混合组
```json
{
  "name": "Claude多版本组",
  "public_model": "claude-mixed",
  "members": [
    "claude-opus-4-8",
    "claude-opus-4-7",
    "claude-sonnet-4-6",
    "claude-haiku-4-5"
  ]
}
```

## API 接口

### 1. 获取分组列表
```http
GET /api/model-groups
```

响应：
```json
{
  "ok": true,
  "groups": [
    {
      "id": "uuid",
      "name": "高性能GPT5组",
      "public_model": "gpt-5-unified",
      "members": ["gpt-5.6-sol", "gpt-5.6-terra"],
      "enabled": true
    }
  ],
  "available_models": ["gpt-5.6-sol", "gpt-5.6-terra", "gpt-5.5"]
}
```

### 2. 创建分组
```http
POST /api/model-groups
Content-Type: application/json

{
  "name": "高性能GPT5组",
  "public_model": "gpt-5-unified",
  "members": ["gpt-5.6-sol", "gpt-5.6-terra", "gpt-5.5"]
}
```

### 3. 更新分组
```http
PUT /api/model-groups/{group_id}
Content-Type: application/json

{
  "name": "新名称",
  "public_model": "gpt-5-unified",
  "members": ["gpt-5.6-sol", "gpt-5.5"],
  "enabled": true
}
```

### 4. 删除分组
```http
DELETE /api/model-groups/{group_id}
```

## 前端界面

### 位置
主页面 → 模型映射区域下方 → "🔗 模型分组 (Model Groups)"

### 功能
1. **分组列表**：显示所有已配置分组，状态标识（启用/禁用）
2. **成员展示**：每个分组显示成员模型序号和名称
3. **创建/编辑**：
   - 输入分组名称和对外模型名
   - 多选框选择成员模型（自动编号显示优先级）
   - 支持编辑现有分组
4. **删除**：确认后删除分组

## 配置文件

分组配置存储在 `api_config.json`：

```json
{
  "api_endpoints": [...],
  "model_aliases": {...},
  "model_groups": [
    {
      "id": "uuid",
      "name": "高性能GPT5组",
      "public_model": "gpt-5-unified",
      "members": ["gpt-5.6-sol", "gpt-5.6-terra", "gpt-5.5"],
      "enabled": true
    }
  ]
}
```

## 兼容性保证

### 1. 不影响现有功能
- ✅ 原有单模型路由完全保留
- ✅ 别名功能正常工作
- ✅ 健康检测、冷却、限流机制不变
- ✅ 端点管理不变

### 2. 冲突检测
- ❌ 对外模型名不能与现有池内模型重复
- ❌ 对外模型名不能与现有别名重复
- ❌ 分组间对外模型名不能重复

### 3. /v1/models 展示
分组的对外模型名会自动出现在 `/v1/models` 列表中，标记为 `owned_by: "api-pool-group"`

## 日志示例

```
[INFO] 检测到分组请求: 高性能GPT5组 (gpt-5-unified), 成员: ['gpt-5.6-sol', 'gpt-5.6-terra', 'gpt-5.5']
[INFO] 分组 [高性能GPT5组] 尝试成员模型: gpt-5.6-sol
[INFO] 收到 API 请求，尝试请求端点 '简直' (模型: gpt-5.6-sol)
[INFO] 端点 '简直' 请求成功 (延迟: 1234ms)
[INFO] 分组 [高性能GPT5组] 成员 gpt-5.6-sol 请求成功
```

失败降级：
```
[INFO] 检测到分组请求: 高性能GPT5组 (gpt-5-unified), 成员: ['gpt-5.6-sol', 'gpt-5.6-terra']
[INFO] 分组 [高性能GPT5组] 尝试成员模型: gpt-5.6-sol
[ERROR] 端点 '简直' 请求失败: timeout
[WARN] 分组 [高性能GPT5组] 成员 gpt-5.6-sol 失败: 模型 gpt-5.6-sol 的所有同名端点均不可用
[INFO] 分组 [高性能GPT5组] 尝试成员模型: gpt-5.6-terra
[INFO] 收到 API 请求，尝试请求端点 '公益Fe' (模型: gpt-5.6-terra)
[INFO] 分组 [高性能GPT5组] 成员 gpt-5.6-terra 请求成功
```

## 注意事项

1. **成员顺序重要**：排在前面的模型会优先被访问，建议把最稳定/最快的模型放前面
2. **模型名匹配**：成员必须是池内已配置的模型名（`public_model` 字段）
3. **健康检测**：每个成员模型内部仍会根据健康状态、优先级选择最优端点
4. **响应模型名**：客户端收到的响应中 `model` 字段是分组的对外名，不是实际使用的成员模型名

## 测试建议

1. **创建测试分组**：包含 2-3 个可用模型
2. **正常请求**：验证第一优先级模型被使用
3. **故障降级**：禁用第一优先级模型的所有端点，验证自动切换到第二个
4. **完全失败**：禁用所有成员模型，验证错误信息正确返回
5. **响应验证**：确认响应中的 `model` 字段是分组的对外名

## 实现细节

### 代码位置
- **数据结构**：`ModelGroup` dataclass (line ~520)
- **路由逻辑**：`APIPool.chat()` 方法开头的分组检测 (line ~1262)
- **API 接口**：`api_handler()` 中的 `/api/model-groups` 路由 (line ~3930)
- **配置加载**：`load_model_groups()` (line ~2491)
- **前端界面**：HTML (line ~4404) + JavaScript (line ~4826)

### 关键函数
- `resolve_model_group(name)` - 检查请求模型是否是分组
- `load_model_groups()` - 从配置加载分组
- `save_config(...)` - 持久化分组到配置文件
- `_sync_to_config()` - 统一配置同步入口

---

**功能已完整实现，可以直接启动服务测试！**
